{
  "name": "training-platform-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright end-to-end tests for the training platform (UI, scoring, forged API calls, scoreboard, and a cluster-gated terminal test).",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
