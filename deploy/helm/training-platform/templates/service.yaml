apiVersion: v1
kind: Service
metadata:
  name: {{ include "training-platform.fullname" . }}
  labels:
    {{- include "training-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "training-platform.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
